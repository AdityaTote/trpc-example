{
    "name": "@repo/db",
    "module": "index.ts",
    "type": "module",
    "private": true,
    "scripts": {
        "db:migrate": "bunx prisma migrate dev",
        "db:generate": "bunx prisma generate"
    },
    "devDependencies": {
        "@types/bun": "latest",
        "prisma": "^6.8.2"
    },
    "peerDependencies": {
        "typescript": "^5"
    },
    "dependencies": {
        "@prisma/client": "^6.8.2"
    }
}
